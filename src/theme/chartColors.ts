/** Stable semantic chart palette shared by every insights primitive. */
export const CHART_COLORS = [
  'var(--gold)',
  'var(--mint)',
  'var(--violet)',
  'var(--blue)',
  'var(--warn)',
  'var(--coral)',
] as const
