# 2026-09-19

## Expense app polish — manage categories modal

- Rebuilt `Web/src/components/CategoryManager.tsx` (opens from Expense page "Manage").
- Killed per-row chrome: bullet dot, vertical rail, group dropdown, pencil/trash emojis.
- One `⋯` overflow menu per row: Edit / Move to group (Other + groups, check mark) / Delete (red, separated).
- Group header: emoji + name + `· count`, sentence-case, 13px muted; own `⋯` menu (Rename/Delete group). "Other" bucket plain, no menu.
- Rows: 44px, hover bg (6% text tint), 12px radius, 16px indent, lucide GripVertical drag handle (faint→bright on hover).
- Inline `+ Add category` per group (autoFocus input, knows its group); pinned `+ New group` footer expands to input, then opens that group's add row.
- Emoji tap on row → centered 6-col picker (36 options); updates name via `updateCategory`.
- Fixed header + scroll list + pinned footer; card 480px, padding 24, groups gap 24.
- Delete confirm copy kept+restyled (server has no expense-redirect; warns "past transactions are kept"; group delete moves cats to Other — server does that).
- CSS: `.erd-manage-*` block in `expense-redesign.css` replacing old light-restyle block; mobile bottom-sheet padding added.
- Checks: typecheck + lint clean, 913 tests pass (121 files).
- Not pushed. Preview needs logged-in localhost (auth is Google/magic-link; no dev bypass).